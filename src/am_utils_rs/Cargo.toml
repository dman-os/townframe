[package]
name = "am_utils_rs"
version.workspace = true
edition.workspace = true

[lints]
workspace = true

[lib]
path = "lib.rs"

[features]
default = ["repo", "iroh"]
repo = [
    "dep:samod",
    "dep:samod-core",
    "samod/tokio",
    "samod/tungstenite",
    "dep:tokio-tungstenite",
    "dep:sqlx",
    "dep:serde_json",
    "dep:irpc"
]

[dependencies]
utils_rs = { workspace = true, features = ["hash"] }

#
# async
tokio = { workspace = true, features = ["sync", "macros", "rt", "rt-multi-thread", "time"] }
tokio-util = { workspace = true }
tokio-tungstenite = { version = "0.27.0", features = ["url"], optional = true }
async-trait.workspace = true

#
# automerge
automerge = { workspace = true }
autosurgeon = { workspace = true }
samod = { workspace = true, optional = true }
samod-core = { workspace = true, optional = true }

#
# encoding
serde.workspace = true
serde_json = { workspace = true, optional = true }
data-encoding.workspace = true
bytes.workspace = true
camino = "1.2.2"
sqlx = { workspace = true, optional = true, features = ["sqlite", "runtime-tokio"] }

#
# errors
thiserror.workspace = true
iroh = { workspace = true, optional = true }
irpc = { version = "0.12.0", optional = true, features = ["derive"] }

#
# logging
tracing.workspace = true
