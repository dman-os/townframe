# `ed` fails cleanly when EDITOR cannot be spawned

The editor spawn happens after the doc is fetched, so a valid doc id is
required first. The spawn error propagates as an eyre report.

<!-- NOTE (resolved by snapshot regeneration): the eyre report shape (Location line + message) is
modeled on e2e/plugs/disable_core_rejected.md; the exact io error text and
source line should be regenerated by the parent's overwrite pass. -->

```console
$ daybook_cli init
$ sh -c 'ID=$("$DAYBOOK_CLI" touch); echo "$ID" > .id'

$ sh -c 'ID=$(cat .id); EDITOR=/nonexistent/editor "$DAYBOOK_CLI" ed "$ID"'
? failed
Error: 
   0: No such file or directory (os error 2)

Location:
   src/daybook_cli/cmds/ed.rs:38

Backtrace omitted. Run with RUST_BACKTRACE=1 environment variable to display it.
Run with RUST_BACKTRACE=full to include source snippets.

```
